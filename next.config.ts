// Ini buat deployment
// /** @type {import('next').NextConfig} */

// const nextConfig = {
//   output: "export",
// }

// module.exports = nextConfig;


// Ini buat dev
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;